# Personal notify skill — holds a private ntfy topic; keep untracked.
# Track skills/notify/sample_SKILL.md as the shareable template instead.
skills/notify/SKILL.md

# Build / run logs (see the caffeinate command in README.md)
*.log
build.log

# OS / editor junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
*.swp
*~
.idea/
.vscode/
